.flexrouter/
__pycache__/
*.py[cod]
.pytest_cache/
dist/
*.egg-info/
.env
dashboard/frontend/node_modules/
dashboard/frontend/dist/

# Contains live provider API keys. Never tracked in this repo's history —
# this makes that explicit instead of relying on every contributor
# remembering not to `git add` it by hand.
flexrouter.yaml
